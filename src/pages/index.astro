---
import { getCollection } from 'astro:content';
import Homepage from '../components/Homepage';
import '../styles/global.css';

// Get blog posts, sorted by date
let blogPosts = [];
try {
  const posts = await getCollection('blog', ({ data }) => !data.draft);
  blogPosts = posts
    .sort((a, b) => new Date(b.data.date) - new Date(a.data.date))
    .map((post) => ({
      title: post.data.title,
      date: new Date(post.data.date).toLocaleDateString('en-US', { year: 'numeric', month: 'short' }),
      tag: post.data.tag,
      url: `/blog/${post.slug}`,
    }));
} catch (e) {
  // No blog posts yet, that's fine
}
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Johnson Jao — Software Engineer & ML Engineer</title>
    <meta name="description" content="Tsung-Han (Johnson) Jao — 5+ years engineering production systems at scale. Software Engineer, ML Engineer, AI Engineer." />
    <!-- Open Graph for LinkedIn / social sharing -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content="Johnson Jao — Software Engineer & ML Engineer" />
    <meta property="og:description" content="5+ years engineering production systems at scale — from distributed data pipelines and ML models to high-concurrency backend services." />
    <meta property="og:url" content="https://johnson00111.github.io/" />
    <meta property="og:image" content="https://johnson00111.github.io/images/og-thumbnail.jpg" />
    <meta property="og:image:width" content="460" />
    <meta property="og:image:height" content="460" />
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Johnson Jao — Software Engineer & ML Engineer" />
    <meta name="twitter:description" content="5+ years engineering production systems at scale." />
    <meta name="twitter:image" content="https://johnson00111.github.io/images/og-thumbnail.jpg" />
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@200;300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;1,400&display=swap" rel="stylesheet" />
  </head>
  <body>
    <Homepage client:load blogPosts={blogPosts} />
  </body>
</html>